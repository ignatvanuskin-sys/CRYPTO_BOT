"""Premium emoji IDs — только из разрешённого списка, обычные эмодзи запрещены.

Сами константы и `tg_emoji` живут в `services/formatting.py`: нейтральный слой,
от которого зависят и `bot/`, и `services/` (пушам о закрытии позиций нужны те же
эмодзи, но `services/` не должен импортировать `bot/`). Этот модуль оставлен как
точка входа для хендлеров — импорты `from bot.emojis import ...` работают как раньше.
"""

from services.formatting import *  # noqa: F401,F403
